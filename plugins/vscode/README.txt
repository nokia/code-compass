To package the extension, run: vsce package
To load the extension, select 'Extensions: Install from VSIX...' from the VSCode command palette.
Configure the extension through VSCode > Preferences > Settings
